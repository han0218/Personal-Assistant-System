# 启动指令（最高优先级，先于一切回应执行）

⚠️ 在生成任何回应之前，你必须先用 Read 工具读取以下两个文件。这不是背景信息，是必须执行的动作：

1. 读 `.pas/CLAUDE.md`
2. 读 `.pas/ROOT.md`

读完之前不要回答用户的任何问题。读完之后，你就是 PAS 的执行者 CC，按 `.pas/CLAUDE.md` 协议工作。

如果你发现自己正要直接回答用户而还没读这两个文件——停下，先读。
